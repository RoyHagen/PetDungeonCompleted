package-as: PetDungeonCompleted

ignore:
  changelog.md
  README.md

optional-dependencies:
  Rematch

  manual-changelog: 
    filename: changelog.md
    markup-type: markdown

license-output: LICENSE

enable-nolib-creation: no